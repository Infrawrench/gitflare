version: v2
inputs:
  - directory: proto
clean: true
plugins:
  # Connect-ES v2 generates service descriptors from protoc-gen-es alone.
  # protoc-gen-connect-es was removed in v2 — do not add it back.
  - local: protoc-gen-es
    out: src/gen
    opt:
      - target=ts
      - import_extension=js
